# Ingress
- we can acess the application throught domain name 

```yaml
apiVersion: v1
kind: ingress
metadata:
    name: myapp-ingress
spec: 
    rules: 
    - host: myapp.com
    http:
        paths:
        - backend: 
            serviceName: my-app-internal-service
            serviceport: 8080

```

