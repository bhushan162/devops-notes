# Private vs Punlic vs Elastic IP

## IPV4 vs IPV6
- IPV4 is most common used IP adress Looks like below 
- **`1.160.10.240`**
- where as IPV6 is used for More IOT Things  and that look like 
- **`3ffe:1900:4545:3:200:f8ff:fe21:67cf`**


## Private vs Public IP
- public ip are the ip that can Unique for eacah devices and it is accecible on internet 
- vs Private IP are more toword internal network that only accecible within private VPN 
- every machine has there unique IP adress 

## Elastic IPs 
- when we start or stop the ec2 instace the public ip gets chnage.
- if you need yo have fixed Public IP for your instance, you need and Elastic OP.
- An elastic Ip is PUblic IPv4IP you own as long as you dont delete it.
- YOU can have 5 Elastic IP in your acoount 
- Overall, ** Try to Avoid Using Elastic IP**:
  - instand, use randome public IP and register a DNSn name to it 
  - use load balancer

# Placement Group
- Sometimes you want controal over the EC@ instance placement statergy
1. Clusteer
2. Spread
3. Partition 

## Cluster
-  ![alt text](image.png)
-  Pros: Great network (10 Gbps bandwidth between instances with Enhanced Networking enabled - recommended)
• Cons: If the AZ fails, all instances fails at the same time
• Use case:
    • Big Data job that needs to complete fast
    • Application that needs extremely low latency and high network throughput

## Spread
![alt text](image-1.png)
• Pros:
    • Can span across AvailabilityZones (AZ)
    • Reduced risk is simultaneousfailure
    • EC2 Instances are on different physical hardware
• Cons:
    • Limited to 7 instances per AZ per placement group
• Use case:
    • Application that needs tomaximize high availability
    • Critical Applications whereeach instance must be isolated from failure from each other 

## Partition 
![alt text](image-2.png)
• Up to 7 partitions per AZ
• Can span across multiple AZs in the same region
• Up to 100s of EC2 instances
• The instances in a partition do not share racks with the instances in the other partitions
• A partition failure can affect many EC2 but won’t affect other partitions
• EC2 instances get access to the partition information as metadata
• Use cases:HDFS, HBase, Cassandra, Kafka 


# Elastic Network Interface (ENI)
- it is a  logical component that reperesent for vertual network  card 
- The ENI can have a following attribute 
  - one primory IPV4 and more secondary IPV4
  - one Elastic IPV4
  - One Public IPV4
  - one or more seccurity Group
  - A mac Adress
- Here we can attach one ENI to TWo or more EC2 in case of failover 
- ENI is bound to specific group 
- ![alt text](image-3.png)


# EC2 Hibernate
- when we stop the any ECC2 then the data is stired but the ram data is not stored that will be destroyed in our normal machine.
- because of that when we next time start machine the ECC2 will take time to boot 
- for this EC2 hibernate come in process 
- now in Hibernate we introduce new step. when ec2 stop --> NORMAL STorage save --> later RAM IS ALSO stored in EBS.
- so because of that next time EC2 INSTANCE Start/boot very fast